export default store => ({ email }) => {  // const language = store.get('language');  return {    steps: [      () => {        return store.callServer('signinWithFacebook', { email });      },      res => {        if (res.error) {          store.addAlert({ name: res.error, timeout: 4000 });          return { error: res.error };        }        return store.route('app', `?user=${res._id}`);      },    ],  };};