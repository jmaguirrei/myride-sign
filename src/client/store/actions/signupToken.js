export default store => () => {  const _id = store.get('user_id');  const password = store.get('signup.password');  const token = store.get('signup.tokenDigits');  return {    steps: [      () => store.callServer('signupFinishRegistration', { _id, password, token }),      res => {        store.set({          'signup.tokenDigits': '',          'signup.buttonPressed': false,        });        if (res.error) return store.addAlert({ name: res.error, timeout: 4000 });        return store.set({          'signup.currentStep': 2,          'signup.name': '',          'signup.email': '',          'signup.password': '',        });      },    ],  };};