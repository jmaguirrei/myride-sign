const MIN_LENGTH = 8;export const password = str => {  const noSpaces = /^\S+$/.test(str);  if (str.length === 0) {    return {      result: false,      message: {        en: 'Password is required',        es: 'Se require la contraseña',      },    };  }  if (!noSpaces) {    return {      result: false,      message: {        en: 'Password can\'t contain white spaces',        es: 'La contraseña no puede contener espacios',      },    };  }  const result = str.length >= MIN_LENGTH;  return {    result,    message: result ? null : {      en: `Password must have at least ${MIN_LENGTH} characters`,      es: `La contraseña debe tener al menos ${MIN_LENGTH} caracteres`,    },  };};