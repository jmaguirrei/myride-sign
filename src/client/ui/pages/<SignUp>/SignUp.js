export default (client, id) => {  const { Alerts } = client.ui.fragments;  const {    SignUpName,    SignUpEmail,    SignUpPassword,    SignUpToken,    SignUpButtons,    SignUpHeadline,    SignUpWelcome,  } = client.ui.pages;  return client.hoc({    id,    state(props, store) {      return {        currentPage: store.get('currentPage'),      };    },    classes: {      container: `        display: block;        position: relative;      `,      step: `        padding: 0 5px;        width: 100%;      `,      noOverflow: `        width: 100%;        overflow: hidden;      `,    },    styles: {      carrousel: currentStep => `        width: 300%;        display: flex;        transform: translateX(${currentStep * -33.33}%);        transition: all .6s ease;      `,    },    render({ styles, state, classes }) {      const { currentStep } = state;      return (        <form id='sign-up' class={classes('container')}>          <Alerts/>          <SignUpHeadline/>          <div class={classes('noOverflow')}>            <div style={styles.carrousel(currentStep)}>              <div id='sign-up-step-0' class={classes('step')}>                <SignUpName/>                <SignUpEmail/>                <SignUpPassword/>              </div>              <div id='sign-up-step-1' class={classes('step')}>                <SignUpToken/>              </div>              <div id='sign-up-step-2' class={classes('step')}>                <SignUpWelcome/>              </div>            </div>          </div>          <SignUpButtons/>        </form>      );    }  });};