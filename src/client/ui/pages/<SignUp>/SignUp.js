export default (client, id) => {  const { Alerts } = client.ui.fragments;  const {    SignUpName,    SignUpEmail,    SignUpPassword,    SignUpToken,    SignUpButtons,    SignUpHeadline,    SignUpWelcome,  } = client.ui.pages;  return client.hoc({    id,    state(props, store) {      return {        currentStep: store.get('signup.currentStep'),      };    },    classes: {      container: `        display: block;        position: relative;      `,      step: `        padding: 0 5px;        width: 100%;      `,    },    styles: {      carrousel: currentStep => `        width: 300%;        display: flex;        ${client.prefix('transform', `translateX(${currentStep * -33.33}%)`)}        transition: all .6s ease;      `,    },    render({ styles, state, classes }) {      return (        <form id='sign-up' class={classes('container')}>          <Alerts/>          <SignUpHeadline/>          <div style={styles.carrousel(state.currentStep)}>            <div id='sign-up-step-0' class={classes('step')}>              <SignUpName/>              <SignUpEmail/>              <SignUpPassword/>            </div>            <div id='sign-up-step-1' class={classes('step')}>              <SignUpToken/>            </div>            <div id='sign-up-step-2' class={classes('step')}>              <SignUpWelcome/>            </div>          </div>          <SignUpButtons/>        </form>      );    }  });};