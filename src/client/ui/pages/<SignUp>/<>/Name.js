export default (client, id) => {  const { Input } = client.ui.components;  return client.hoc({    id,    state(props, store) {      return {        name: store.get('signup.name'),      };    },    actions(props, store) {      return {        onTextChanged: e => {          const name = e.target.value;          store.set({ 'signup.name': name });        },      };    },    classes: {      wrapper: `        padding: 5px 10px;      `,    },    render({ state, classes, actions, utils }) {      const { name } = state;      return (        <div class={classes.wrapper}>          <Input {...{            id: 'signup-name-input',            placeholder: utils.localize({ en: 'Your name', es: 'Tu nombre' }),            value: name,            onTextChanged: actions.onTextChanged,            autoFocus: true,            autocomplete: 'name',          }}/>        </div>      );    }  });};