export default (client, id) => {  const { SignupControl } = client.lib;  return client.hoc({    id,    state(props, store) {      return {        buttonPressed: store.get('signup.buttonPressed'),        currentStep: store.get('signup.currentStep'),      };    },    actions(props, store) {      return {        onClickButton: () => SignupControl(store),        onGoToSignin: () => store.set({ currentPage: 'signin' }),      };    },    styles: {      button: (pressed, currentStep) => `        opacity: ${pressed ? 0.5 : 1};        width: ${currentStep >= 1 ? 70 : 98}%;      `,    },    classes: {      wrapper: `        padding: 20px 10px;        display: flex;        flex-flow: column;        justify-content: center;      `,      button: `        border-radius: 7px;        color: white;        text-align: center;        font-size: 18px;        padding: 10px;        background: ${client.lib.Colors.GREEN_SIGNUP};        cursor: pointer;      `,      alreadyRegistered: `        padding: 10px 20px;        font-size: 13px;        user-select: none;        flex: 1;        opacity: 1;        text-align: right;        cursor: pointer;      `,    },    render({ classes, state, actions, styles, utils }) {      const { currentStep, buttonPressed } = state;      return (        <div class={classes.wrapper}>          <div            class={classes.button}            style={styles.button(buttonPressed, currentStep)}            onclick={actions.onClickButton}          >            {              ({                0: utils.localize({ en: 'Create Account', es: 'Crear Cuenta' }),                1: utils.localize({ en: 'Validate Token', es: 'Validar Token' }),                2: utils.localize({ en: 'Open the App', es: 'Ir a la App' }),              })[currentStep]            }          </div>          <div class={classes.alreadyRegistered} onclick={actions.onGoToSignin}>            {              utils.localize({                en: 'Already registered? Login',                es: '¿Ya estás registrado? Entrar',              })            }          </div>        </div>      );    }  });};