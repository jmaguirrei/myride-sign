export default (client, id) => {  const { Input } = client.ui.components;  return client.hoc({    id,    state(props, store) {      return {        email: store.get('signup.email'),      };    },    actions(props, store) {      return {        onTextChanged: e => {          const email = e.target.value;          store.set({ 'signup.email': email, 'forgot.email': email });        },      };    },    classes: {      wrapper: `        padding: 5px 10px;      `,    },    render({ state, classes, actions, utils }) {      const { email } = state;      return (        <div class={classes('wrapper')}>          <Input            type='email'            placeholder={utils.localize({ en: 'Your email', es: 'Tu email' })}            value={email}            onTextChanged={actions.onTextChanged}            autocomplete='email'          />        </div>      );    }  });};