export default (client, id) => {  const { Alerts } = client.ui.fragments;  const {    SignInEmail,    SignInPassword,    SignInButtons,    SignInHeadline,  } = client.ui.pages;  return client.hoc({    id,    classes: {      container: `        display: block;        padding: 0 5px;        position: relative;      `,    },    render({ classes }) {      return (        <form id='sign-in' class={classes('container')}>          <Alerts/>          <SignInHeadline/>          <SignInEmail/>          <SignInPassword/>          <SignInButtons/>        </form>      );    }  });};