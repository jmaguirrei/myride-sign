export default (client, id) => {  const { Input } = client.ui.components;  return client.hoc({    id,    state(props, store) {      return {        email: store.get('signin.email'),      };    },    actions(props, store) {      return {        onTextChanged: e => {          const email = e.target.value;          store.set({ 'signin.email': email, 'forgot.email': email });        },      };    },    classes: {      wrapper: `        padding: 1px 10px;      `,    },    render({ state, classes, actions, utils }) {      const { email } = state;      return (        <div class={classes('wrapper')}>          <Input            placeholder={utils.localize({ en: 'Registered email', es: 'Email registrado' })}            value={email}            onTextChanged={actions.onTextChanged}            autocomplete='email'            type='email'            autoFocus={true}            id='signin-email-input'          />        </div>      );    }  });};