export const RootControl = store => {  const onMenuNav = page => {    if (page === store.get('currentPage')) {      store.toggle('isMenuOpen');    } else {      store.set({        currentPage: page,        isMenuOpen: current => !current,      });      store.route(page);    }  };  const onGoHome = () => {    store.toggle('isMenuOpen');    store.route('home');  };  return {    menuOptions: [      { name: 'Iniciar Sesión', className: 'link', onclick: () => onMenuNav('signin') },      { name: 'Crear una cuenta', className: 'link', onclick: () => onMenuNav('signup') },      { name: 'Recuperar Password', className: 'link', onclick: () => onMenuNav('forgot') },      { name: 'Home', className: 'link', onclick: onGoHome },    ],  };};